# ── Database (Prisma) ────────────────────────────────────────────
# DEV (SQLite, zero-setup — runs immediately):
DATABASE_URL="file:./dev.db"

# PROD (PostgreSQL / Neon) — switch schema.prisma provider to "postgresql":
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
